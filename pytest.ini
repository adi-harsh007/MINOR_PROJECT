[pytest]
testpaths = tests
addopts = -q
markers =
    slow: exercises real model inference (loads the checkpoint)
    requires_model: needs models/latest.pt present, without being slow
filterwarnings =
    ignore::DeprecationWarning:sqlalchemy.*
